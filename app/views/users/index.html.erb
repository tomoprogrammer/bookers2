<h1>New book</h1>
<!-- form_with部分 -->
<%= form_with model: @book, url:'/books', local:true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :Title %>
  <h4>本文</h4>
  <%= f.text_area :Opinion %>
  <%= f.submit 'Create Book' %>
<% end %>